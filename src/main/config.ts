/**
 * TEST : à `true`, le lancement ne vérifie plus les mods / hash (extra mods, SHA-512).
 * Remettre à `false` avant toute diffusion publique.
 */
export const SKIP_MOD_INTEGRITY_FOR_LAUNCH = true
