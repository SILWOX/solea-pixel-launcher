# Déploiement Netlify : répertoire de publication = site statique.
[build]
  publish = "Website"

# En-têtes utiles pour le cache des assets (le HTML reste court).
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=86400"
